const REFRESH_HOUR_GERMANY=17;let currentFilter='all';let reports=[];let jobs=[];async function fetchJson(path){const response=await fetch(`${path}?v=${Date.now()}`,{cache:'no-store'});if(!response.ok)throw new Error(`Failed to load ${path}`);return response.json()}async function loadData(){const [r,j]=await Promise.all([fetchJson('./data/reports.json'),fetchJson('./data/jobs.json')]);reports=r.reports;jobs=j.jobs;const select=document.getElementById('dates');select.innerHTML=reports.slice().reverse().map(r=>`<option value="${r.date}">${new Date(r.date+'T12:00:00').toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'})}</option>`).join('');show(reports.at(-1)?.date)}function show(date){const r=reports.find(x=>x.date===date);if(!r)return;const ids=new Set(r.jobIds);let list=jobs.filter(j=>ids.has(j.id)&&j.status==='active');const fresh=list.filter(j=>j.fresh).length;document.getElementById('stats').innerHTML=`<div class="stat"><strong>${list.length}</strong><span>ACTIVE JOBS</span></div><div class="stat"><strong>${fresh}</strong><span>FRESH TODAY</span></div><div class="stat"><strong>${list.length-fresh}</strong><span>PREVIOUSLY LISTED</span></div><div class="stat"><strong>${r.closingWithin7Days}</strong><span>CLOSING ≤ 7 DAYS</span></div>`;renderJobs(list);document.getElementById('reportDate').textContent=new Date(date+'T12:00:00').toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'})}function renderJobs(list){let filtered=currentFilter==='all'?list:list.filter(j=>j.category.includes(currentFilter));filtered.sort((a,b)=>b.match-a.match);document.getElementById('jobs').innerHTML=filtered.length?filtered.map(j=>{const days=j.deadline?Math.ceil((new Date(j.deadline+'T23:59:59')-new Date())/86400000):null;return `<article class="job"><div class="head"><div><h2>${j.title}</h2><div class="company">${j.company}</div><div class="meta">${j.location} · ${j.category.join(' · ')}</div></div><div><div class="score">${j.match}%</div><div class="small">CV MATCH</div></div></div><div class="tags"><span class="tag ${j.fresh?'fresh':'old'}">${j.fresh?'FRESH':'Previously listed'}</span>${days!==null?`<span class="tag ${days<=7?'deadline':''}">${days>0?days+' days remaining':'Deadline passed'}</span>`:''}</div><ul>${j.fit.map(x=>`<li>✓ ${x}</li>`).join('')}</ul>${j.gaps?`<div class="small"><b>Gaps:</b> ${j.gaps.join(' · ')}</div>`:''}<p><a class="apply" href="${j.url}" target="_blank" rel="noopener">Apply / View listing →</a></p><div class="small">Status verified for this report.</div></article>`}).join(''):'<div class="card empty">No matching opportunities for this filter.</div>'}document.querySelectorAll('.filter').forEach(b=>b.addEventListener('click',()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');currentFilter=b.dataset.filter;show(document.getElementById('dates').value)}));document.getElementById('dates').addEventListener('change',e=>show(e.target.value));function nextRefresh(){const now=new Date();const target=new Date(now);target.setHours(17,0,0,0);if(now>=target)target.setDate(target.getDate()+1);const diff=target-now;const h=Math.floor(diff/36e5),m=Math.floor(diff%36e5/6e4),s=Math.floor(diff%6e4/1e3);document.getElementById('countdown').textContent=`${String(h).padStart(2,'0')}:${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`}setInterval(nextRefresh,1000);nextRefresh();loadData();
